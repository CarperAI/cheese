Frontend:
	- Need to make it actually pretty
	- Need to make it automatically refresh when no data is currently being presented
	- Need a nice start/intermediate screen before/between tasks
Backend:
	- Need to add safety checks to Pipeline to verify nothing gets lost/all data gets labelled
		-> namely crashes, and if a client decides to leave when they have a task